# Test 1: Without directive - should show warnings
alias sensor d0
l r0 sensor Pressure
l r1 sensor Temperature
# r0 and r1 are assigned but never read - should warn

# Test 2: With directive - should NOT show warnings
#IgnoreRegisterWarnings
l r2 sensor Setting
l r3 sensor Activate
# r2 and r3 are assigned but never read - should NOT warn due to directive
